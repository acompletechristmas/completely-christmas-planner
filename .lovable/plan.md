## Recovery plan

I will fix this by treating your uploaded screenshot as the locked target layout, not as inspiration.

### What will change
1. Restore the homepage hero to match the uploaded version:
   - logo top-left
   - menu top-right
   - heading and gold script text in the same place
   - countdown tag left of the tree
   - gold “LET'S START CHRISTMAS” button under the tree and above the cream section
   - cream planner section starting where it does in your screenshot

2. Replace only the bow on the countdown tag:
   - use the new realistic satin bow
   - position it where the old bow sat on the tag
   - keep the tag, tree, button, text, and section placement unchanged

3. I will not redesign, resize, or move other elements.

### Verification before I say it is done
- I will compare the live mobile preview against your uploaded screenshot at the same mobile width.
- I will check that the gold button and planner cards remain clickable.
- I will only report completion after the screenshot matches the locked layout as closely as the current assets allow.

### Technical scope
- Likely only `src/routes/index.tsx` will be touched.
- If the realistic bow asset pointer needs correcting, only the existing bow asset reference will be adjusted.
- No navigation, planner pages, copy, fonts, or backend changes.